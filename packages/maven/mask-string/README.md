# mask-string

Hide all but the final characters of a string.

Maven coordinates: `io.github.xmori:mask-string:1.0.1`. Java 17 or newer.

Use the static methods in `io.github.xmori.maskstring.MaskString`.
