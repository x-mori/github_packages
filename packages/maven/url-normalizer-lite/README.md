# url-normalizer-lite

Normalize HTTP URLs and default ports.

Maven coordinates: `io.github.xmori:url-normalizer-lite:1.0.0`. Java 17 or newer.

Use the static methods in `io.github.xmori.urlnormalizerlite.UrlNormalizerLite`.
