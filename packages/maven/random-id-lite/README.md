# random-id-lite

Create cryptographically random readable IDs.

Maven coordinates: `io.github.xmori:random-id-lite:1.0.0`. Java 17 or newer.

Use the static methods in `io.github.xmori.randomidlite.RandomIdLite`.
