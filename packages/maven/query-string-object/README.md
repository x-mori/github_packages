# query-string-object

Parse and encode query parameters with repeated keys.

Maven coordinates: `io.github.xmori:query-string-object:1.0.1`. Java 17 or newer.

Use the static methods in `io.github.xmori.querystringobject.QueryStringObject`.
