# human-file-size

Format byte counts in SI or binary units.

Maven coordinates: `io.github.xmori:human-file-size:1.0.1`. Java 17 or newer.

Use the static methods in `io.github.xmori.humanfilesize.HumanFileSize`.
