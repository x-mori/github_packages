# human-duration

Format milliseconds as days, hours, minutes, and seconds.

Maven coordinates: `io.github.xmori:human-duration:1.0.1`. Java 17 or newer.

Use the static methods in `io.github.xmori.humanduration.HumanDuration`.
