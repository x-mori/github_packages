# date-range

Generate inclusive day, week, or month dates.

Maven coordinates: `io.github.xmori:date-range:1.0.0`. Java 17 or newer.

Use the static methods in `io.github.xmori.daterange.DateRange`.
