# email-normalizer

Trim email whitespace and lowercase only the domain.

Maven coordinates: `io.github.xmori:email-normalizer:1.0.0`. Java 17 or newer.

Use the static methods in `io.github.xmori.emailnormalizer.EmailNormalizer`.
