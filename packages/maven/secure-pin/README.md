# secure-pin

Generate a cryptographically random numeric PIN.

Maven coordinates: `io.github.xmori:secure-pin:1.0.0`. Java 17 or newer.

Use the static methods in `io.github.xmori.securepin.SecurePin`.
