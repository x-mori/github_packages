# strip-tracking-params

Remove common tracking query parameters from a URL.

Maven coordinates: `io.github.xmori:strip-tracking-params:1.0.0`. Java 17 or newer.

Use the static methods in `io.github.xmori.striptrackingparams.StripTrackingParams`.
