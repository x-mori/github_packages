# age-from-date

Calculate full years since a birth date.

Maven coordinates: `io.github.xmori:age-from-date:1.0.1`. Java 17 or newer.

Use the static methods in `io.github.xmori.agefromdate.AgeFromDate`.
