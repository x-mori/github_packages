# relative-time-lite

Describe an instant relative to another instant.

Maven coordinates: `io.github.xmori:relative-time-lite:1.0.0`. Java 17 or newer.

Use the static methods in `io.github.xmori.relativetimelite.RelativeTimeLite`.
